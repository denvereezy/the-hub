{{#facilitator}}
    <h3>Results:</h3>
    <table class="table table-striped">
        <thead class="thead-inverse">
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Value</th>
            </tr>
        </thead>

        {{#each values}}
            <tr>
                <td>{{title}}</td>
                <td>{{description}}</td>
                <td>{{value}}</td>
            </tr>
        {{/each}}
    </table>
    <button class="btn btn-success" onclick="goBack()">back</button>
    <form class="pull-right" method="post" action="/questionnaire/{{base_questionnaire_id}}/rolledup-answers">
        <input type="submit" id="button" class="btn btn-primary" value="Release" />
    </form>
{{/facilitator}}
